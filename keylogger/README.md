# Keylogger (minimal)

This repository contains a simple `keylogger` script in `keylogger/script.py`.

To run:

```python3
python3 keylogger/script.py --logfile ./keylog.txt
```

Press `ESC` to stop the listener.

Note: Use this script responsibly and only with explicit authorization.
